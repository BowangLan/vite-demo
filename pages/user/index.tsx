import { NextPage } from "next/types";

const UserIndex: NextPage = () => {
  return (
    <div className="h-full flex justify-center items-center">
      <h2 className="text-xl font-serif tracking-wider">User Index Page</h2>
    </div>
  );
};

export default UserIndex;
